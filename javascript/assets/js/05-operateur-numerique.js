/* -------------------- OPERATEUR ARITHMETIQUE -------------------- */

// +, -, *, /, %, **

// Declarer une constante operation affecte l'addition de 678686 et 78789
// Affiche en console le resultat du calcul est OPERATION

let operation = 678686 + 78789
console.log(`Le résultat du calcul est ${operation}`)

// Meme calcul avec la soustraction 

const operation2 = 678686 - 78789
console.log(`Le résultat du calcul est ${operation2}`)

// Meme calcul avec la multiplication

const operation3 = 678686 * 78789
console.log(`Le résultat du calcul est ${operation3}`)

// Meme calcul avec la division

const operation4 = 678686 / 78789
console.log(`Le résultat du calcul est ${operation4}`)

// Meme calcul avec le modulo (Modulo = le reste de la division)

const operation5 = 10 % 3
console.log(`Le résultat du calcul est ${operation5}`)

operation = 10 ** 3
console.log(operation)

// Demande a l'utilisateur un nombre
// Demande a l'utilisateur un deuxième nombre

// si le nombre est divisible par le premier 
    // affichez ce nombre est un multiple du deuxième 
// sinon
    // ce n'est pas un multiple



